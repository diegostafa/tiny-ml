
(* comment
   in many
   lines *)

// line comment

(*
let f x y = (x, y) in
let g x = f x x
in let f = fun x -> (if x then 1 else 2, x + 1) in () *)

// f: x: ('a -> Int -> 'b) -> y: 'a -> 'b
(* let f x y = x y 1 in f *)


// let f x y z = x y z 1 in f

let rec fib n = fib (n - 1) + fib (n - 2)



in fib
// let x = 1 + 1.5 in x + 1.5
